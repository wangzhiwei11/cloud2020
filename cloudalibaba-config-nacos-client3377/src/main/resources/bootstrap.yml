server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848 #nacos作为注册中心地址
      config:
        server-addr: http://localhost:8848 #nacos作为配置中心地址
        file-extension: yaml #指定yaml格式配置
